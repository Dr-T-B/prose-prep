# Repository Identity

`prose-prep` is the clean staging/development repo for the Prose app.

`prose-craft-aid` remains the previous/source repo until migration is complete.

Production Supabase is `szdgsmpxtifrcmwelqfo` and must not be used for staging work.

Staging Supabase is `nxlxunygoccbnzdopqna`.

Type generation is blocked until the staging schema exists.

No production database work is authorised from this repo.
